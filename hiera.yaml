---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "site/%{site}/cluster/%{cluster}.yaml"
      - "site/%{site}.yaml"
      - "role/%{role}.yaml"
      - "common.yaml"
